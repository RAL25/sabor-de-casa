generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// O schema será preenchido conforme a modelagem em docs/05-modelagem-de-dados.md
